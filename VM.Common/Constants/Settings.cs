namespace VM.Common.Constants
{
    public class Settings
    {
        public const int ACCESS_TOKEN_EXPIRATION_MINUTES = 60; // 1 HOUR
        public const int REFRESH_TOKEN_EXPIRATION_MINUTES = 3 * 24 * 60; // 3 DAYS
    }
}
